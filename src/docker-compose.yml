version: '3.4'

services:
  catalogdb:
    image: mongo

  api.catalog:
    image: ${DOCKER_REGISTRY-}apicatalog
    build:
      context: .
      dockerfile: API.Catalog/Dockerfile

volumes:
  mongo_data:
